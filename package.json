{
  "name": "vercel-image-api",
  "version": "1.0.0",
  "description": "A serverless function to add text to images",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",           // Express (if used for API)
    "canvas": "^2.9.0",             // For image manipulation
    "node-fetch": "^2.6.7"          // CommonJS-compatible version
  },
  "engines": {
    "node": "20.x"                  // Ensure the correct Node.js version is used
  },
  "license": "MIT"
}
